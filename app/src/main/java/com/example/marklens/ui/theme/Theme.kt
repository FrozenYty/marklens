package com.example.marklens.ui.theme

import androidx.compose.ui.graphics.Color

// ── MarkLens Color System ──
// Industrial precision tool × academic paper warmth.

val Ink = Color(0xFF1C1C2E)          // Primary — deep academic blue-black
val Paper = Color(0xFFFAF8F5)        // Background — warm paper white
val MarkRed = Color(0xFFE63946)      // Region box — bold grading red
val SoftGreen = Color(0xFF2A9D8F)    // Selected region — confident green
val Amber = Color(0xFFF4A261)        // Drag preview — warm amber
val Slate = Color(0xFF64748B)        // Secondary text & icons
val SurfaceWhite = Color(0xFFFFFFFF) // Card surfaces
val InkTranslucent = Color(0xCC1C1C2E) // Semi-transparent overlay
